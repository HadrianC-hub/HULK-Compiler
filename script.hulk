type Node1 (value, node)
{
    value = value;
    node = node;
}

type Node2 (val, node)
{
    val = val;
    node = node;
}

let o = new Node1 (2, new Node2 (10, new Node1(5, Null))), z = o.node in print (z.node);